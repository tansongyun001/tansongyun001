#include <stdio.h>
int old(int a,int k)
{
	int n;
	old(int a,int k)=(n-1)*k;
	return Old(int a,int k)+a;
	
}
int main (void)
{
	int n,a,k;
	if(n<=0||k>100||n<2)
	  printf("Wrong Number\n");
	else
	  printf("%d",old(int a,int k));
	return 0;
}
